plot(heatmap_sig)
>>>>>>> main
dev.off()
png(filename = "plots/volcano_d0_d2.png",
width = 16,
height = 9,
units = "cm",
res = 400)
plot(volcano_0_2)
dev.off()
<<<<<<< HEAD
png(filename = "plots/volcano_d0_d3.png",
width = 16,
height = 9,
units = "cm",
res = 400)
plot(volcano_0_3)
dev.off()
png(filename = "plots/relativeChanged_FL.png",
width = 16,
height = 9,
units = "cm",
res = 400)
plot(relativeChanged_FL)
dev.off()
png(filename = "plots/relativeChanged_all.png",
width = 16,
height = 9,
units = "cm",
res = 400)
plot(relativeChanged_all)
dev.off()
absolute_norm_values <- cleanDecoyCounts %>%
select(-Length) %>%
pivot_longer(cols = 2:(1+nSamples),names_to = "sample") %>%
merge(coldata, by = "sample")
View(absolute_norm_values)
View(cleanDecoyCounts)
base::unique(cleanDecoyCounts$Geneid)
stableDecoyIDs <- bas::unique(cleanDecoyCounts$Geneid)
stableDecoyIDs <- base::unique(cleanDecoyCounts$Geneid)
write_csv(stableDecoyIDs,
"intermediates/stable_decoy_ids.csv")
stableDecoyIDs
stableDecoyIDs <- as.data.frame(Geneid = base::unique(cleanDecoyCounts$Geneid))
=======
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(DESeq2)
library(matrixStats)
library(vegan)
decoyCountFiles <- list.files("../../out/counts",
pattern = "*decoy_unique_featureCounts.txt$",
full.names = TRUE)
coldata <- read.csv("../../samples.csv")
# Filter decoy geneids to not include rRNAs
ribosomalIDs <- read.delim("../../references/nucleus_apicoplast/ToxoDB-68_TgondiiRH88.gff",
skip = 64,
header = F) %>%
filter(V3 == "rRNA") %>%
select(V9) %>%
mutate(V9 = sub(".*ID=", "", V9),
V9 = sub("_t.*", "", V9))
getwd()
setwd("/home/sascha/data/Documents/toxo_pipeline_rework/scripts/R/")
decoyCountFiles <- list.files("../../out/counts",
pattern = "*decoy_unique_featureCounts.txt$",
full.names = TRUE)
coldata <- read.csv("../../samples.csv")
# Filter decoy geneids to not include rRNAs
ribosomalIDs <- read.delim("../../references/nucleus_apicoplast/ToxoDB-68_TgondiiRH88.gff",
skip = 64,
header = F) %>%
filter(V3 == "rRNA") %>%
select(V9) %>%
mutate(V9 = sub(".*ID=", "", V9),
V9 = sub("_t.*", "", V9))
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(DESeq2)
library(matrixStats)
library(vegan)
decoyCountFiles <- list.files("../../out/counts",
pattern = "*decoy_unique_featureCounts.txt$",
full.names = TRUE)
coldata <- read.csv("../../samples.csv")
# Filter decoy geneids to not include rRNAs
ribosomalIDs <- read.delim("../../references/nucleus_apicoplast/ToxoDB-68_TgondiiRH88.gff",
skip = 64,
header = F) %>%
filter(V3 == "rRNA") %>%
select(V9) %>%
mutate(V9 = sub(".*ID=", "", V9),
V9 = sub("_t.*", "", V9))
decoyCountFiles <- list.files("../../out/counts",
pattern = "*decoy_unique_featureCounts.txt$",
full.names = TRUE)
coldata <- read.csv("../../samples.csv")
# Filter decoy geneids to not include rRNAs
ribosomalIDs <- read.delim("../../references/decoy/ToxoDB-68_TgondiiRH88.gff",
skip = 64,
header = F) %>%
filter(V3 == "rRNA") %>%
select(V9) %>%
mutate(V9 = sub(".*ID=", "", V9),
V9 = sub("_t.*", "", V9))
ribosomalIDs <- unique(ribosomalIDs$V9)
decoyCounts <- read.delim(decoyCountFiles[1], header = T, skip = 1) %>%
select(Geneid, Length, last_col())
for (file in decoyCountFiles[2:length(decoyCountFiles)]){
counts <- read.delim(file, header = T, skip = 1) %>%
select(Geneid, last_col())
decoyCounts <- left_join(decoyCounts, counts, by = "Geneid")
}
# Filter out all genes with less than 10 reads in all samples and remove rRNA samples
cleanDecoyCounts <- decoyCounts  %>%
filter(if_all(-Geneid, ~ .x >= 10),
!Geneid %in% ribosomalIDs)
colnames(cleanDecoyCounts) <- base::gsub("out.filtered.",
"",
colnames(cleanDecoyCounts))
colnames(cleanDecoyCounts) <- base::gsub("_decoy_filtered.bam",
"",
colnames(cleanDecoyCounts))
colnames(cleanDecoyCounts) <- base::gsub("\\.",
"_",
colnames(cleanDecoyCounts))
>>>>>>> main
stableDecoyIDs <- data.frame(Geneid = base::unique(cleanDecoyCounts$Geneid))
write_csv(stableDecoyIDs,
"intermediates/stable_decoy_ids.csv")
View(stableDecoyIDs)
<<<<<<< HEAD
stable_decoy_HPR <- read.csv(file = "/media/molgenuser/DATA/helicase_toxo_srna_pipeline/scripts/R/stable_decoy_ids.csv")
View(stable_decoy_HPR)
stable_decoy_HPR <- read.csv(file = "/media/molgenuser/DATA/helicase_toxo_srna_pipeline/scripts/R/stable_decoy_ids.csv") %>%
mutate(helicase = ifelse(Geneid %in% stableDecoyIDs$Geneid, T, F))
View(stable_decoy_HPR)
=======
>>>>>>> main
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(DESeq2)
library(matrixStats)
library(vegan)
decoyCountFiles <- list.files("../../out/counts/decoy",
pattern = "*decoy_featureCounts.txt$",
full.names = TRUE)
nSamples <- length(decoyCountFiles)
coldata <- read.csv("../../samples.csv")
# Filter decoy geneids to not include rRNAs
ribosomalIDs <- read.delim("../../references/decoy/ToxoDB-68_TgondiiRH88.gff",
# Filter decoy geneids to not include rRNAs
ribosomalIDs <- read.delim("../../references/decoy/ToxoDB-68_TgondiiRH88.gff",
skip = 64,
header = F) %>%
filter(V3 == "rRNA") %>%
select(V9) %>%
mutate(V9 = sub(".*ID=", "", V9),
V9 = sub("_t.*", "", V9))
ribosomalIDs <- unique(ribosomalIDs$V9)
# Filter decoy geneids to not include rRNAs
ribosomalIDs <- read.delim("../../references/decoy/ToxoDB-68_TgondiiRH88.gff",
skip = 64,
header = F) %>%
filter(V3 == "rRNA") %>%
select(V9) %>%
mutate(V9 = sub(".*ID=", "", V9),
V9 = sub("_t.*", "", V9))
ribosomalIDs
getwd()
decoyCountFiles
decoyCountFiles <- list.files("../../out/counts/decoy",
pattern = "*decoy_featureCounts.txt$",
full.names = TRUE)
nSamples <- length(decoyCountFiles)
coldata <- read.csv("../../samples.csv")
coldata
View(coldata)
ribosomalIDs
View(ribosomalIDs)
View(ribosomalIDs)
# Filter decoy geneids to not include rRNAs
ribosomalIDs <- read.delim("../../references/decoy/ToxoDB-68_TgondiiRH88.gff",
skip = 64,
header = F) %>%
filter(V3 == "rRNA") %>%
select(V9) %>%
mutate(V9 = sub(".*ID=", "", V9),
V9 = sub("_t.*", "", V9))
ribosomalIDs <- unique(ribosomalIDs$V9)
ribosomalIDs
decoyCounts <- read.delim(decoyCountFiles[1], header = T, skip = 1) %>%
select(Geneid, Length, last_col())
decoyCounts <- read.delim(decoyCountFiles[1], header = T, skip = 1)
decoyCounts <- read.delim(decoyCountFiles[1], header = T) %>%
select(Geneid, Length, last_col())
decoyCounts <- read.delim(decoyCountFiles[1])
View(decoyCounts)
decoyCountFiles <- list.files("../../out/counts/decoy",
pattern = "*decoy_featureCounts.txt$",
full.names = TRUE)
nSamples <- length(decoyCountFiles)
coldata <- read.csv("../../samples.csv")
# Filter decoy geneids to not include rRNAs
ribosomalIDs <- read.delim("../../references/decoy/ToxoDB-68_TgondiiRH88.gff",
skip = 64,
header = F) %>%
filter(V3 == "rRNA") %>%
select(V9) %>%
mutate(V9 = sub(".*ID=", "", V9),
V9 = sub("_t.*", "", V9))
ribosomalIDs <- unique(ribosomalIDs$V9)
decoyCounts <- read.delim(decoyCountFiles[1], header = T, skip = 1) %>%
select(Geneid, Length, last_col())
read.delim(decoyCountFiles[1], header = T, skip = 1)
decoyCounts <- read.delim(decoyCountFiles[1]) %>%
select(Geneid, Length, last_col())
read.delim(decoyCountFiles[1])
decoyCountFiles[1]
decoyCounts <- read.delim(decoyCountFiles[1], header = F, skip = 1)
View(decoyCounts)
decoyCountFiles <- list.files("../../out/counts/decoy",
pattern = "*decoy_featureCounts.txt$",
full.names = TRUE)
nSamples <- length(decoyCountFiles)
coldata <- read.csv("../../samples.csv")
# Filter decoy geneids to not include rRNAs
ribosomalIDs <- read.delim("../../references/decoy/ToxoDB-68_TgondiiRH88.gff",
skip = 64,
header = F) %>%
filter(V3 == "rRNA") %>%
select(V9) %>%
mutate(V9 = sub(".*ID=", "", V9),
V9 = sub("_t.*", "", V9))
ribosomalIDs <- unique(ribosomalIDs$V9)
decoyCounts <- read.delim(decoyCountFiles[1], header = F, skip = 1)
select(Geneid, Length, last_col())
decoyCountFiles <- list.files("../../out/counts/decoy",
pattern = "*decoy_featureCounts.txt$",
full.names = TRUE)
nSamples <- length(decoyCountFiles)
coldata <- read.csv("../../samples.csv")
# Filter decoy geneids to not include rRNAs
ribosomalIDs <- read.delim("../../references/decoy/ToxoDB-68_TgondiiRH88.gff",
skip = 64,
header = F) %>%
filter(V3 == "rRNA") %>%
select(V9) %>%
mutate(V9 = sub(".*ID=", "", V9),
V9 = sub("_t.*", "", V9))
ribosomalIDs <- unique(ribosomalIDs$V9)
decoyCounts <- read.delim(decoyCountFiles[1], header = F, skip = 1) %>%
select(Geneid, Length, last_col())
View(decoyCounts)
decoyCountFiles <- list.files("../../out/counts/decoy",
pattern = "*decoy_featureCounts.txt$",
full.names = TRUE)
nSamples <- length(decoyCountFiles)
coldata <- read.csv("../../samples.csv")
# Filter decoy geneids to not include rRNAs
ribosomalIDs <- read.delim("../../references/decoy/ToxoDB-68_TgondiiRH88.gff",
skip = 64,
header = F) %>%
filter(V3 == "rRNA") %>%
select(V9) %>%
mutate(V9 = sub(".*ID=", "", V9),
V9 = sub("_t.*", "", V9))
ribosomalIDs <- unique(ribosomalIDs$V9)
decoyCounts <- read.delim(decoyCountFiles[1], header = T, skip = 1) %>%
select(Geneid, Length, last_col())
for (file in decoyCountFiles[2:length(decoyCountFiles)]){
counts <- read.delim(file, header = T, skip = 1) %>%
select(Geneid, last_col())
decoyCounts <- left_join(decoyCounts, counts, by = "Geneid")
}
# Filter out all genes with less than 10 reads in all samples and remove rRNA samples
cleanDecoyCounts <- decoyCounts  %>%
filter(if_all(-Geneid, ~ .x >= 10),
!Geneid %in% ribosomalIDs,
Length <= 300)
colnames(cleanDecoyCounts) <- base::gsub("out.filtered.",
"",
colnames(cleanDecoyCounts))
colnames(cleanDecoyCounts) <- base::gsub("_decoy_filtered.bam",
"",
colnames(cleanDecoyCounts))
colnames(cleanDecoyCounts) <- base::gsub("\\.",
"_",
colnames(cleanDecoyCounts))
stableDecoyIDs <- data.frame(Geneid = base::unique(cleanDecoyCounts$Geneid))
write_csv(stableDecoyIDs,
"intermediates/stable_decoy_ids.csv")
absolute_norm_values <- cleanDecoyCounts %>%
select(-Length) %>%
pivot_longer(cols = 2:(1+nSamples),names_to = "sample") %>%
merge(coldata, by = "sample") %>%
group_by(sample_name) %>%
summarise(value = sum(value),
con = con) %>%
unique()
ggplot(as.data.frame(absolute_norm_values),
aes(x=con, y=value, color=sample_name)) + geom_point() + theme_bw()
base::saveRDS(absolute_norm_values,
"intermediates/absolute_norm_values.RDS")
M <- cleanDecoyCounts %>%
select(-Length) %>%
column_to_rownames("Geneid") %>%
as.matrix()
# features x samples
# PCA on ALL decoys (sanity)
pca <- prcomp(t(M), scale. = FALSE)
ve <- round(100 * (pca$sdev^2 / sum(pca$sdev^2)), 1)
autoplot <- function(pc, col) {
df <- data.frame(pc$x[,1:2], group = coldata[[col]])
ggplot(df, aes(PC1, PC2, color = group)) +
geom_point(size = 3) +
labs(x = paste0("PC1 (", ve[1], "%)"),
y = paste0("PC2 (", ve[2], "%)"),
title = "Decoy PCA (selected decoys)") +
theme_bw()
}
p1 <- autoplot(pca, "con")
knitr::opts_chunk$set(echo = FALSE,
message = FALSE,
warning = FALSE,
fig.align = "center")
packages <- c("plotly", "tidyverse", "ggrepel", "circlize")
installed <- rownames(installed.packages())
to_install <- setdiff(packages, installed)
if (length(to_install) > 0) {
message("Installing missing packages: ", paste(to_install, collapse = ", "))
install.packages(to_install, repos = "https://cloud.r-project.org")
}
library(plotly)
library(tidyverse)
library(ggrepel)
library(ComplexHeatmap)
library(circlize)
library(edgeR)
countFiles <- list.files("../../out/counts/mito/shortFeatures/sense",
pattern = "*mito_featureCounts.txt$",
full.names = TRUE)
nSample <- length(countFiles)
absoluteNormalizators <- base::readRDS("intermediates/absolute_norm_values.RDS")
coldata <- read.csv("../../samples.csv")
mitoCounts <- read.delim(countFiles[1], header = T, skip = 1) %>%
select(Geneid, last_col())
for (file in countFiles[2:length(countFiles)]){
counts <- read.delim(file, header = T, skip = 1) %>%
select(Geneid, last_col())
mitoCounts <- left_join(mitoCounts, counts, by = "Geneid")
}
View(mitoCounts)
# Filter out all genes with less than 20 reads in all samples
cleanCounts <- mitoCounts %>%
filter(if_all(-Geneid, ~ .x >= 20)) %>%
# Remove 'rRNA' from Geneids
mutate(Geneid = base::gsub(".rRNA", "", Geneid)) %>%
pivot_longer(cols = 1:nSample+1,names_to = "sample") %>%
mutate(sample = base::gsub("out.filtered.", "", sample),
sample = base::gsub("_mito_filtered.bam", "", sample),
sample = base::gsub("\\.", "_", sample)) %>%
merge(coldata, by = "sample") %>%
select(sample = sample_name, Geneid, value) %>%
mutate(sample = base::gsub("\\-", "_", sample)) %>%
pivot_wider(names_from = sample,
values_from = value)
View(cleanCounts)
longCounts <- list.files("../../out/counts/mito/longFeatures",
pattern = "*mito_featureCounts.txt$",
full.names = TRUE)
longCounts
mitoCounts <- read.delim(countFiles[1], header = T, skip = 1) %>%
select(Geneid, last_col())
View(mitoCounts)
longCounts <- read.delim(longFiles[1], header = T, skip = 1) %>%
select(Geneid, last_col())
longFiles <- list.files("../../out/counts/mito/longFeatures",
pattern = "*mito_featureCounts.txt$",
full.names = TRUE)
longCounts <- read.delim(longFiles[1], header = T, skip = 1) %>%
select(Geneid, last_col())
for (file in longFiles[2:length(longFiles)]){
counts <- read.delim(file, header = T, skip = 1) %>%
select(Geneid, last_col())
longCounts <- left_join(longCounts, counts, by = "Geneid")
}
View(longCounts)
cleanLongCount <- longCounts %>%
pivot_longer(cols = 1:nSample+1,names_to = "sample") %>%
mutate(sample = base::gsub("out.filtered.", "", sample),
sample = base::gsub("_mito_filtered.bam", "", sample),
sample = base::gsub("\\.", "_", sample)) %>%
merge(coldata, by = "sample") %>%
select(sample = sample_name, Geneid, value) %>%
mutate(sample = base::gsub("\\-", "_", sample)) %>%
pivot_wider(names_from = sample,
values_from = value)
View(cleanLongCount)
countFiles <- list.files("../../out/counts/mito/shortFeatures/sense",
pattern = "*mito_featureCounts.txt$",
full.names = TRUE)
nSample <- length(countFiles)
absoluteNormalizators <- base::readRDS("intermediates/absolute_norm_values.RDS")
coldata <- read.csv("../../samples.csv")
mitoCounts <- read.delim(countFiles[1], header = T, skip = 1) %>%
select(Geneid, last_col())
for (file in countFiles[2:length(countFiles)]){
counts <- read.delim(file, header = T, skip = 1) %>%
select(Geneid, last_col())
mitoCounts <- left_join(mitoCounts, counts, by = "Geneid")
}
longFiles <- list.files("../../out/counts/mito/longFeatures",
pattern = "*mito_featureCounts.txt$",
full.names = TRUE)
# Filter out all genes with less than 20 reads in all samples
cleanShortCounts <- mitoCounts %>%
filter(if_all(-Geneid, ~ .x >= 20)) %>%
# Remove 'rRNA' from Geneids
mutate(Geneid = base::gsub(".rRNA", "", Geneid)) %>%
pivot_longer(cols = 1:nSample+1,names_to = "sample") %>%
mutate(sample = base::gsub("out.filtered.", "", sample),
sample = base::gsub("_mito_filtered.bam", "", sample),
sample = base::gsub("\\.", "_", sample)) %>%
merge(coldata, by = "sample") %>%
select(sample = sample_name, Geneid, value) %>%
mutate(sample = base::gsub("\\-", "_", sample)) %>%
pivot_wider(names_from = sample,
values_from = value)
longCounts <- read.delim(longFiles[1], header = T, skip = 1) %>%
select(Geneid, last_col())
for (file in longFiles[2:length(longFiles)]){
counts <- read.delim(file, header = T, skip = 1) %>%
select(Geneid, last_col())
longCounts <- left_join(longCounts, counts, by = "Geneid")
}
cleanLongCounts <- longCounts %>%
pivot_longer(cols = 1:nSample+1,names_to = "sample") %>%
mutate(sample = base::gsub("out.filtered.", "", sample),
sample = base::gsub("_mito_filtered.bam", "", sample),
sample = base::gsub("\\.", "_", sample)) %>%
merge(coldata, by = "sample") %>%
select(sample = sample_name, Geneid, value) %>%
mutate(sample = base::gsub("\\-", "_", sample)) %>%
pivot_wider(names_from = sample,
values_from = value)
cleanCounts <- rbind(cleanShortCounts, cleanLongCounts)
View(cleanCounts)
# Set example genes throughout the document
example_genes = c("RNA36", "RNA33_F", "RNA2", "RNA30", "RNA18", "SSUD", "SSUE_2", "SSUE_F", "SSUE_1")
example_plot <- cleanCounts %>%
filter(Geneid %in% example_genes) %>%
pivot_longer(cols = 1:nSample+1,names_to = "sample_name") %>%
mutate(sample_name = base::gsub("\\_", "-", sample_name)) %>%
merge(coldata, by = "sample_name")
ggplot(example_plot,
aes(x = con,
y = value,
color = Geneid)) +
geom_point(show.legend = FALSE) +
theme_bw() +
scale_y_log10() +
labs(x = "Condition",
y = expression(log[10]("Absolute Reads")),
title = "Raw Reads per Condition") +
facet_wrap(~Geneid,
scales = "free_y")
# mitoCounts: matrix of mito features (rows) x samples (columns), raw counts
mitoCounts <- cleanCounts %>%
mutate(Geneid = paste0(Geneid, "XX")) %>%
column_to_rownames("Geneid")
# group: factor of conditions for each sample (length = ncol(mitoCounts))
group <- factor(base::gsub("_Rep.*", "", colnames(mitoCounts)), levels = unique(base::gsub("_Rep.*", "", colnames(mitoCounts))))
# anchor A_j:   numeric vector of length ncol(mitoCounts)
#               IMPORTANT: A_j must exclude mitochondrial reads; >0 in every sample
Aj <- absoluteNormalizators$value
# 1) Prepare DGEList with offset-only normalization (NO TMM)
Aj <- Aj * (1e6 / median(Aj))       # optional rescale for stability
y  <- DGEList(mitoCounts)
y$samples$lib.size <- Aj
y$samples$group <- group
# 2) Design with all groups (no intercept), pr_0 will be the baseline in contrasts
design <- model.matrix(~ 0 + group)
colnames(design) <- levels(group)
# 3) Dispersion + fit (QL pipeline)
y  <- estimateDisp(y, design)
fit <- glmQLFit(y, design)
# 4) Define contrasts: (first) - (pr_0)
CT <- makeContrasts(
par0_par3 = par_Day_3 - par_Day_0,
par0_pr0 = pr_Day_0 - par_Day_0,
pr0_pr1 = pr_Day_1    - pr_Day_0,   # logFC > 0 => pr_1 > pr_0
pr0_pr2 = pr_Day_2    - pr_Day_0,
pr0_pr3 = pr_Day_3    - pr_Day_0,
levels = design
)
# 5) Run tests and collect results
run_one <- function(fit, contr_name) {
tt <- glmQLFTest(fit, contrast = CT[, contr_name])
out <- topTags(tt, n = Inf)$table
out$contrast <- contr_name
out
}
res_list <- lapply(colnames(CT), function(nm) run_one(fit, nm))
res <- do.call(rbind, res_list)
# res has logFC (relative to the anchor), PValue, FDR for each feature/bin and contrast
split(res, res$contrast) |>
lapply(function(df) df[order(df$FDR), ]) -> by_contrast
res_clean <- res %>%
rownames_to_column("Geneid") %>%
mutate(Geneid = sub("XX.*", "", Geneid))
